{
    "tslint.configFile": "tslint.json",
    "editor.codeActionsOnSave": {
        "source.fixAll.tslint": true
    },
    "files.exclude": {
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/**.js": {
            "when": "$(basename).tsx"
        }
    }
}